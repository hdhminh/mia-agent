{
  "name": "Sub-workflow: Google Calendar - Find Event",
  "nodes": [
    {
      "parameters": {},
      "id": "9dd0277a-0958-4b0d-9e41-ecc667ecd391",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const source = $('Execute Workflow Trigger').item.json || {};\nconst payload = source.payload || {};\nconst message = source.message || payload.message || payload.body?.message || {};\nfunction normalize(str) {\n  return String(str || '')\n    .toLowerCase()\n    .normalize('NFD')\n    .replace(/[\\u0300-\\u036f]/g, '')\n    .replace(/\\u0111/g, 'd')\n    .replace(/\\u0110/g, 'D');\n}\nconst raw = String(source.rawText || message.text || payload.text || '').trim();\nconst text = normalize(source.text || raw);\nconst chatId = source.chatId || message.chat?.id || payload.chatId || '';\nconst calendarId = source.calendarId || payload.calendarId || 'kuminhuynhdoan@gmail.com';\nfunction pad(n) { return String(n).padStart(2, '0'); }\nfunction iso(d, h = 0, m = 0, end = false) {\n  return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(h)}:${pad(m)}:${end ? '59' : '00'}+07:00`;\n}\nfunction startOfDay(d) { const x = new Date(d); x.setHours(0,0,0,0); return x; }\nfunction endOfDay(d) { const x = new Date(d); x.setHours(23,59,59,0); return x; }\nfunction nextWeekday(base, targetDow, nextWeek = false) {\n  const d = startOfDay(base);\n  let diff = (targetDow - d.getDay() + 7) % 7;\n  if (diff === 0) diff = 7;\n  if (nextWeek) diff += 7;\n  d.setDate(d.getDate() + diff);\n  return d;\n}\nconst now = new Date();\nlet afterDate = now;\nlet beforeDate = new Date(now.getTime() + 1000 * 60 * 60 * 24 * 30);\nif (/cuoi tuan nay/.test(text)) {\n  afterDate = nextWeekday(now, 6, false);\n  beforeDate = endOfDay(new Date(afterDate.getTime() + 86400000));\n} else if (/cuoi tuan sau/.test(text)) {\n  afterDate = nextWeekday(now, 6, true);\n  beforeDate = endOfDay(new Date(afterDate.getTime() + 86400000));\n} else if (/tuan sau/.test(text)) {\n  const monday = nextWeekday(now, 1, true);\n  afterDate = monday;\n  beforeDate = endOfDay(new Date(monday.getTime() + 6 * 86400000));\n} else if (/ngay mai|mai/.test(text)) {\n  afterDate = startOfDay(new Date(now.getTime() + 86400000));\n  beforeDate = endOfDay(afterDate);\n} else {\n  const weekdayMatch = text.match(/thu\\s*(2|3|4|5|6|7)|chu nhat/);\n  if (weekdayMatch) {\n    let targetDow = weekdayMatch[1] ? Number(weekdayMatch[1]) - 1 : 0;\n    const nextWeek = /tuan sau/.test(text);\n    afterDate = nextWeekday(now, targetDow, nextWeek);\n    beforeDate = endOfDay(afterDate);\n  }\n}\nconst query = raw\n  .replace(/^(tim lich|tim su kien|search event|find event|xem lich voi|lich)\\s*/i, '')\n  .replace(/\\b(hom nay|ngay mai|mai|tuan sau|tuan nay|cuoi tuan nay|cuoi tuan sau|thu\\s*[2-7]|chu nhat)\\b/gi, '')\n  .replace(/\\s+/g, ' ')\n  .trim();\nreturn [{ json: { ...source, payload, message, rawText: raw, text, chatId, calendarId, query, after: iso(afterDate), before: iso(beforeDate, 23, 59, true) } }];"
      },
      "id": "4d80a1d9-d0c6-4ce5-a943-76e72ba7aad9",
      "name": "Chuan Bi Tim Lich",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        240,
        0
      ]
    },
    {
      "parameters": {
        "operation": "getAll",
        "calendar": {
          "__rl": true,
          "mode": "id",
          "value": "={{ $json.calendarId || $json.payload?.calendarId || 'kuminhuynhdoan@gmail.com' }}"
        },
        "limit": 5,
        "options": {
          "timeMin": "={{ $json.after }}",
          "timeMax": "={{ $json.before }}",
          "query": "={{ $json.query }}"
        }
      },
      "id": "2c39f18d-bb70-4a6c-85e9-58aba35a58f5",
      "name": "Google Calendar Find",
      "type": "n8n-nodes-base.googleCalendar",
      "typeVersion": 1,
      "position": [
        512,
        0
      ],
      "alwaysOutputData": true,
      "credentials": {
        "googleCalendarOAuth2Api": {
          "id": "1Lgsbjfibo4xVURe",
          "name": "Google Calendar account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "function toDate(value) {\n  const date = new Date(value);\n  return Number.isNaN(date.getTime()) ? null : date;\n}\nfunction formatDate(date) {\n  return new Intl.DateTimeFormat('vi-VN', { timeZone: 'Asia/Ho_Chi_Minh', day: '2-digit', month: '2-digit', year: 'numeric' }).format(date);\n}\nfunction formatTime(date) {\n  return new Intl.DateTimeFormat('vi-VN', { timeZone: 'Asia/Ho_Chi_Minh', hour: '2-digit', minute: '2-digit', hour12: false }).format(date);\n}\nfunction formatRange(startValue, endValue) {\n  const startDate = toDate(startValue);\n  const endDate = toDate(endValue);\n  if (!startDate) return startValue || '';\n  if (!endDate) return `${formatTime(startDate)}, ${formatDate(startDate)}`;\n  const sameDay = startDate.toLocaleDateString('en-CA', { timeZone: 'Asia/Ho_Chi_Minh' }) === endDate.toLocaleDateString('en-CA', { timeZone: 'Asia/Ho_Chi_Minh' });\n  if (sameDay) return `${formatTime(startDate)} - ${formatTime(endDate)}, ${formatDate(startDate)}`;\n  return `${formatTime(startDate)}, ${formatDate(startDate)} → ${formatTime(endDate)}, ${formatDate(endDate)}`;\n}\nconst source = $('Chuan Bi Tim Lich').item.json;\nconst rows = $input.all().map(item => item.json || {}).filter((event) => event && Object.keys(event).length > 0 && (event.id || event.summary || event.start || event.end));\nlet text = `🔎 <b>Kết quả tìm lịch</b>\\n<i>${source.query || source.rawText}</i>\\n\\n`;\nif (rows.length === 0) {\n  text += 'Không tìm thấy sự kiện phù hợp.';\n} else {\n  for (let i = 0; i < rows.length; i++) {\n    const event = rows[i];\n    const title = event.summary || 'Không có tiêu đề';\n    const start = event.start?.dateTime || event.start?.date || '';\n    const end = event.end?.dateTime || event.end?.date || '';\n    text += `<b>${i + 1}. ${title}</b>\\n🕒 ${formatRange(start, end)}`;\n    if (event.htmlLink) text += `\\n🔗 ${event.htmlLink}`;\n    text += '\\n\\n';\n  }\n}\nreturn [{ json: { chatId: source.chatId, text: text.trim() } }];"
      },
      "id": "a4d33306-938e-430b-8771-7ba650461a92",
      "name": "Format Tim Lich",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        768,
        0
      ]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [
        [
          {
            "node": "Chuan Bi Tim Lich",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Chuan Bi Tim Lich": {
      "main": [
        [
          {
            "node": "Google Calendar Find",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Google Calendar Find": {
      "main": [
        [
          {
            "node": "Format Tim Lich",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Tim Lich": {
      "main": [
        []
      ]
    }
  }
}