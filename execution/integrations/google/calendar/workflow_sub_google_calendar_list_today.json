{
  "name": "Sub-workflow: Google Calendar - List Today",
  "nodes": [
    {
      "parameters": {},
      "id": "1cc3dd00-c179-4403-9831-b08048948040",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        272,
        384
      ]
    },
    {
      "parameters": {
        "jsCode": "const source = $('Execute Workflow Trigger').item.json || {};\nconst payload = source.payload || {};\nconst message = source.message || payload.message || payload.body?.message || {};\nconst chatId = source.chatId || message.chat?.id || payload.chatId || '';\nconst calendarId = source.calendarId || payload.calendarId || 'primary';\nconst now = new Date();\nconst start = `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-${String(now.getDate()).padStart(2,'0')}T00:00:00+07:00`;\nconst end = `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-${String(now.getDate()).padStart(2,'0')}T23:59:59+07:00`;\nreturn [{ json: { ...source, payload, message, chatId, calendarId, after: start, before: end, heading: 'Lịch hôm nay' } }];"
      },
      "id": "9cbe773e-b27e-4006-8dff-1cfeb34bed24",
      "name": "Chuan Bi Lich Hom Nay",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        512,
        384
      ]
    },
    {
      "parameters": {
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "googleCalendarOAuth2Api",
        "url": "=https://www.googleapis.com/calendar/v3/calendars/{{ encodeURIComponent($json.calendarId) }}/events",
        "sendQueryParameters": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "timeMin",
              "value": "={{ $json.after }}"
            },
            {
              "name": "timeMax",
              "value": "={{ $json.before }}"
            },
            {
              "name": "singleEvents",
              "value": "true"
            },
            {
              "name": "orderBy",
              "value": "startTime"
            },
            {
              "name": "maxResults",
              "value": "={{ $json.limit || 5 }}"
            }
          ]
        },
        "options": {}
      },
      "id": "769ba034-e6a7-4418-986d-96cb0db4add7",
      "name": "Google Calendar List",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [
        784,
        384
      ],
      "credentials": {
        "googleCalendarOAuth2Api": {
          "id": "1Lgsbjfibo4xVURe",
          "name": "Google Calendar account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "function toDate(value) {\n  const date = new Date(value);\n  return Number.isNaN(date.getTime()) ? null : date;\n}\nfunction formatDate(date) {\n  return new Intl.DateTimeFormat('vi-VN', { timeZone: 'Asia/Ho_Chi_Minh', day: '2-digit', month: '2-digit', year: 'numeric' }).format(date);\n}\nfunction formatTime(date) {\n  return new Intl.DateTimeFormat('vi-VN', { timeZone: 'Asia/Ho_Chi_Minh', hour: '2-digit', minute: '2-digit', hour12: false }).format(date);\n}\nfunction formatRange(startValue, endValue) {\n  const startDate = toDate(startValue);\n  const endDate = toDate(endValue);\n  if (!startDate) return startValue || '';\n  if (!endDate) return `${formatTime(startDate)}, ${formatDate(startDate)}`;\n  const sameDay = startDate.toLocaleDateString('en-CA', { timeZone: 'Asia/Ho_Chi_Minh' }) === endDate.toLocaleDateString('en-CA', { timeZone: 'Asia/Ho_Chi_Minh' });\n  if (sameDay) return `${formatTime(startDate)} - ${formatTime(endDate)}, ${formatDate(startDate)}`;\n  return `${formatTime(startDate)}, ${formatDate(startDate)} → ${formatTime(endDate)}, ${formatDate(endDate)}`;\n}\nconst source = $('Chuan Bi Lich Hom Nay').item.json;\nconst owner = String($env.OWNER_DISPLAY_NAME || 'User').trim();\nconst rawData = $input.first().json || {};\nconst rows = (rawData.items || []).filter((event) => event && (event.id || event.summary || event.start || event.end)).slice(0, 3);\nlet text = '';\nif (rows.length === 0) {\n  text = `Hôm nay ${owner} chưa có lịch nào cả.`;\n} else {\n  text = `Hôm nay ${owner} có mấy lịch như sau:`;\n  const links = [];\n  for (let i = 0; i < rows.length; i++) {\n    const event = rows[i];\n    const title = event.summary || 'Không có tiêu đề';\n    const start = event.start?.dateTime || event.start?.date || '';\n    const end = event.end?.dateTime || event.end?.date || '';\n    const link = event.htmlLink || '';\n    text += `\\n${i + 1}. ${title} — ${formatRange(start, end)}`;\n    if (link) links.push(link);\n  }\n  if (links.length) {\n    text += '\\n\\nMở nhanh lịch:';\n    links.slice(0, 3).forEach((link, index) => {\n      text += `\\n${index + 1}. ${link}`;\n    });\n  }\n}\nreturn [{ json: { chatId: source.chatId, text: text.trim() } }];"
      },
      "id": "61986c24-91ea-442e-b3e8-91bb4902d1a8",
      "name": "Format Danh Sach Lich",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1040,
        384
      ]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [
        [
          {
            "node": "Chuan Bi Lich Hom Nay",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Chuan Bi Lich Hom Nay": {
      "main": [
        [
          {
            "node": "Google Calendar List",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Google Calendar List": {
      "main": [
        [
          {
            "node": "Format Danh Sach Lich",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Danh Sach Lich": {
      "main": [
        []
      ]
    }
  }
}