{
  "name": "Sub-workflow: Google Gmail - Search Email",
  "nodes": [
    {
      "parameters": {},
      "id": "gmail-search-trigger",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [0, 0]
    },
    {
      "parameters": {
        "jsCode": "const source = $('Execute Workflow Trigger').item.json || {};\nconst payload = source.payload || {};\nconst message = source.message || payload.message || {};\nconst chatId = source.chatId || message.chat?.id || '';\nconst raw = String(source.rawText || message.text || '').trim();\nconst query = raw.replace(/^(tim mail|tìm mail|tim email|tìm email|search mail|search email|tim kiem mail|tìm kiếm mail|tim kiem email|tìm kiếm email)\\s*/i, '').trim();\nconst hasQuery = Boolean(query);\nconst guidance = '🔎 <b>Tìm kiếm email</b>\\n\\nVí dụ: tìm mail từ Google';\nreturn [{ json: { ...source, chatId, query, hasQuery, response: hasQuery ? '' : guidance } }];"
      },
      "id": "gmail-search-prepare",
      "name": "Chuan Bi Tim Email",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [240, 0]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.hasQuery }}",
              "value2": true
            }
          ]
        }
      },
      "id": "gmail-search-check",
      "name": "Du Du Lieu Tim?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [480, 0]
    },
    {
      "parameters": {
        "operation": "getAll",
        "limit": 5,
        "filters": {
          "search": "={{ $json.query }}"
        }
      },
      "id": "4c96d70c-ffd5-4d6f-a62b-518f9174d219",
      "name": "Gmail Search",
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.1,
      "position": [736, -80],
      "alwaysOutputData": true,
      "webhookId": "bd759b5f-077c-4515-abae-abc46a3cddb1",
      "credentials": {
        "gmailOAuth2": {
          "id": "5bVi6VSAG8L6X4DP",
          "name": "Gmail account 2"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const source = $('Chuan Bi Tim Email').item.json;\n\nconst escapeHtml = (value = '') => String(value)\n  .replace(/&/g, '&amp;')\n  .replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;')\n  .replace(/\\\"/g, '&quot;');\n\nconst pickFirst = (...values) => {\n  for (const value of values) {\n    if (value === undefined || value === null) continue;\n    if (typeof value === 'string' && value.trim()) return value.trim();\n    if (typeof value === 'number' && Number.isFinite(value)) return String(value);\n  }\n  return '';\n};\n\nconst formatDate = (value) => {\n  if (value === undefined || value === null || value === '') return '';\n  const normalized = /^\\d+$/.test(String(value)) ? Number(value) : value;\n  const date = new Date(normalized);\n  return Number.isNaN(date.getTime()) ? '' : date.toLocaleString('vi-VN', { timeZone: 'Asia/Ho_Chi_Minh' });\n};\n\nconst rows = $input.all().map((i) => i.json || {}).filter((e) => e.id || e.subject || e.Subject || e.from || e.From || e.snippet);\nlet text = '🔎 <b>Kết quả tìm email</b>\\n\\n';\nif (!rows.length) {\n  text += `Không tìm thấy email phù hợp với: <i>${escapeHtml(source.query)}</i>`;\n} else {\n  rows.forEach((e, i) => {\n    const subject = pickFirst(e.subject, e.Subject, e.payload?.headers?.find((h) => h.name === 'Subject')?.value) || 'Không có tiêu đề';\n    const from = pickFirst(e.from?.value?.[0]?.name, e.from?.value?.[0]?.address, e.From, e.from, e.payload?.headers?.find((h) => h.name === 'From')?.value) || 'Không rõ';\n    const date = formatDate(pickFirst(e.date, e.Date, e.internalDate));\n    text += `<b>${i + 1}. ${escapeHtml(subject)}</b>\\n👤 ${escapeHtml(from)}\\n`;\n    if (date) text += `🕒 ${escapeHtml(date)}\\n`;\n    text += '\\n';\n  });\n}\nreturn [{ json: { chatId: source.chatId, text: text.trim() } }];"
      },
      "id": "gmail-search-format",
      "name": "Format Tim Email",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [960, -80]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ 'https://api.telegram.org/bot' + $env.TELEGRAM_BOT_TOKEN + '/sendMessage' }}",
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            { "name": "chat_id", "value": "={{ $json.chatId }}" },
            { "name": "text", "value": "={{ $json.text ?? $json.response }}" },
            { "name": "parse_mode", "value": "HTML" },
            { "name": "disable_web_page_preview", "value": "true" }
          ]
        },
        "options": {}
      },
      "id": "gmail-search-send",
      "name": "Gui Gmail",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.1,
      "position": [1200, 0]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [[{ "node": "Chuan Bi Tim Email", "type": "main", "index": 0 }]]
    },
    "Chuan Bi Tim Email": {
      "main": [[{ "node": "Du Du Lieu Tim?", "type": "main", "index": 0 }]]
    },
    "Du Du Lieu Tim?": {
      "main": [
        [{ "node": "Gmail Search", "type": "main", "index": 0 }],
        [{ "node": "Gui Gmail", "type": "main", "index": 0 }]
      ]
    },
    "Gmail Search": {
      "main": [[{ "node": "Format Tim Email", "type": "main", "index": 0 }]]
    },
    "Format Tim Email": {
      "main": [[{ "node": "Gui Gmail", "type": "main", "index": 0 }]]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  }
}
