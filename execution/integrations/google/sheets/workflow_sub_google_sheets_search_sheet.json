{
  "nodes": [
    {
      "parameters": {},
      "id": "0f834f33-b0e6-4378-b35b-ca894da6dbaf",
      "name": "Execute Workflow Trigger",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1,
      "position": [
        -736,
        80
      ]
    },
    {
      "parameters": {
        "jsCode": "function normalize(str) {\n  return String(str || '')\n    .toLowerCase()\n    .normalize('NFD')\n    .replace(/[\\u0300-\\u036f]/g, '')\n    .replace(/đ/g, 'd')\n    .replace(/Đ/g, 'D')\n    .trim();\n}\n\nfunction getTelegramContext(source) {\n  const payload = source.payload || {};\n  const message = source.message || payload.message || payload.body?.message || {};\n  const chatId = source.chatId || message.chat?.id || payload.chatId || '';\n  const rawText = String(\n    source.rawText || source.text || message.text || message.caption || payload.text || '' || payload.query || ''\n  ).trim();\n  return { payload, message, chatId, rawText };\n}\n\nfunction cleanQuery(value) {\n  return String(value || '')\n    .trim()\n    .replace(/^[\\\"'“”‘’]+|[\\\"'“”‘’]+$/g, '')\n    .replace(/\\s+/g, ' ');\n}\n\nconst source = $('Execute Workflow Trigger').item.json || {};\nconst args = source.args || source.payload?.args || {};\nconst ctx = getTelegramContext(source);\nconst raw = ctx.rawText;\nconst text = normalize(raw);\nconst prefixes = ['tim sheet', 'search sheet', 'tim bang tinh'];\nlet body = raw;\nfor (const prefix of prefixes) {\n  if (text.startsWith(prefix + ' ')) {\n    body = raw.slice(prefix.length + 1).trim();\n    break;\n  }\n}\n\nconst query = cleanQuery(\n  source.query ||\n  source.sheetName ||\n  source.targetName ||\n  source.fileName ||\n  args.query ||\n  args.sheetName ||\n  args.targetName ||\n  ctx.payload.query ||\n  ctx.payload.sheetName ||\n  body\n);\nconst limit = Number(source.limit || args.limit || source.payload?.limit || 5) || 5;\nconst folderId = String(source.folderId || args.folderId || source.payload?.folderId || '').trim();\n\nreturn [{\n  json: {\n    ...source,\n    payload: ctx.payload,\n    message: ctx.message,\n    chatId: ctx.chatId,\n    rawText: raw,\n    text,\n    query,\n    sheetName: query,\n    targetName: cleanQuery(source.targetName || args.targetName || query),\n    folderId,\n    limit,\n    hasTarget: Boolean(query),\n    response: query ? '' : '📊 <b>Tim Google Sheet</b>\\n\\nVi du: <code>tim sheet Chi tieu</code>'\n  }\n}];"
      },
      "id": "9bebb89f-6681-4e50-9f6b-d71e6d54d6e8",
      "name": "Prepare Action",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -496,
        80
      ]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.hasTarget }}",
              "value2": true
            }
          ]
        }
      },
      "id": "7dee52a2-f2d4-4d91-9a68-5b57de1f8d42",
      "name": "Co Tu Khoa Tim?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 1,
      "position": [
        -256,
        80
      ]
    },
    {
      "parameters": {
        "resource": "fileFolder",
        "searchMethod": "query",
        "queryString": "={{ \"name contains '\" + String($json.query || '').replace(/'/g, \"\\\\'\") + \"' and mimeType = 'application/vnd.google-apps.spreadsheet' and trashed = false\" }}",
        "limit": "={{ $json.limit }}",
        "filter": {
          "driveId": {
            "mode": "list",
            "value": "My Drive"
          },
          "whatToSearch": "all",
          "includeTrashed": false,
          "folderId": {
            "__rl": true,
            "mode": "id",
            "value": "={{ $json.folderId || 'root' }}"
          }
        },
        "options": {
          "fields": [
            "*"
          ]
        }
      },
      "id": "311ec67a-36f2-4cc7-98d9-fb1170a854c4",
      "name": "Drive Search Sheets",
      "type": "n8n-nodes-base.googleDrive",
      "typeVersion": 3,
      "position": [
        0,
        0
      ],
      "alwaysOutputData": true,
      "credentials": {
        "googleDriveOAuth2Api": {
          "id": "xa6nA0L0pPqTF0jz",
          "name": "Google Drive account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const source = $('Prepare Action').item.json || {};\nconst rows = $input.all().map((item) => item.json || {}).filter((item) => item.id || item.name).slice(0, 3);\nconst query = source.query || '';\n\nlet text = '';\nif (!rows.length) {\n  text = query\n    ? `Mia chưa tìm thấy bảng tính nào thật sự khớp với \"${query}\".`\n    : 'Mia chưa tìm thấy bảng tính phù hợp.';\n} else {\n  text = query\n    ? `Mia tìm thấy ${rows.length} bảng tính khá khớp với \"${query}\":`\n    : `Mia tìm thấy ${rows.length} bảng tính phù hợp:`;\n  const links = [];\n  rows.forEach((sheet, index) => {\n    const name = sheet.name || 'Không rõ tên';\n    const link = sheet.webViewLink || (sheet.id ? `https://docs.google.com/spreadsheets/d/${sheet.id}/edit` : '');\n    const modified = sheet.modifiedTime\n      ? new Date(sheet.modifiedTime).toLocaleString('vi-VN', {\n          timeZone: 'Asia/Ho_Chi_Minh',\n          hour12: false,\n        })\n      : '';\n    text += `\\n${index + 1}. ${name}`;\n    if (modified) text += ` — cập nhật ${modified}`;\n    if (link) links.push(link);\n  });\n  if (links.length) {\n    text += '\\n\\nMở bảng tính:';\n    links.slice(0, 3).forEach((link, index) => {\n      text += `\\n${index + 1}. ${link}`;\n    });\n  }\n}\n\nreturn [{ json: { chatId: source.chatId, text: text.trim() } }];"
      },
      "id": "81eb4972-c727-4a53-9120-2259186cf714",
      "name": "Format Action",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        224,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const source = $json || {};\n\nfunction decodeEntities(value = '') {\n  return String(value)\n    .replace(/&nbsp;/gi, ' ')\n    .replace(/&amp;/gi, '&')\n    .replace(/&lt;/gi, '<')\n    .replace(/&gt;/gi, '>')\n    .replace(/&quot;/gi, '\"')\n    .replace(/&#39;/gi, \"'\");\n}\n\nfunction htmlToPlainText(value = '') {\n  return decodeEntities(String(value || ''))\n    .replace(/<br\\s*\\/?>/gi, '\\n')\n    .replace(/<\\/p>/gi, '\\n\\n')\n    .replace(/<\\/div>/gi, '\\n')\n    .replace(/<\\/li>/gi, '\\n')\n    .replace(/<li>/gi, '- ')\n    .replace(/<\\/h\\d>/gi, '\\n')\n    .replace(/<a[^>]*href=\"([^\"]+)\"[^>]*>(.*?)<\\/a>/gi, '$2: $1')\n    .replace(/<[^>]+>/g, ' ')\n    .replace(/[ \\t]+/g, ' ')\n    .replace(/\\s+([,.;:!?])/g, '$1')\n    .replace(/\\n\\s+/g, '\\n')\n    .replace(/\\n{3,}/g, '\\n\\n')\n    .trim();\n}\n\nconst target =\n  source.query ||\n  source.docName ||\n  source.sheetName ||\n  source.fileName ||\n  source.folderName ||\n  source.targetName ||\n  source.title ||\n  source.summary ||\n  source.rawText ||\n  source.text ||\n  '';\n\nlet text = htmlToPlainText(source.response || source.text || source.error || '');\nif (!text) {\n  text = target\n    ? `Mia chưa xử lý được yêu cầu với \"${target}\". Bạn kiểm tra lại thông tin đầu vào giúp Mia nhé.`\n    : 'Mia chưa đủ thông tin để xử lý yêu cầu này.';\n}\n\nreturn [{\n  json: {\n    ok: false,\n    tool: source.tool || source.workflowKey || '',\n    chatId: source.chatId || '',\n    text,\n    links: [],\n    result: {},\n    meta: {\n      target: String(target || '').trim(),\n      reason: source.hasTarget === false || source.isReady === false ? 'missing_required_input' : 'not_found_or_not_applicable'\n    }\n  }\n}];\n"
      },
      "id": "return-empty-branch",
      "name": "Return Empty Branch",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        464,
        240
      ]
    }
  ],
  "connections": {
    "Execute Workflow Trigger": {
      "main": [
        [
          {
            "node": "Prepare Action",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Action": {
      "main": [
        [
          {
            "node": "Co Tu Khoa Tim?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Co Tu Khoa Tim?": {
      "main": [
        [
          {
            "node": "Drive Search Sheets",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Return Empty Branch",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Drive Search Sheets": {
      "main": [
        [
          {
            "node": "Format Action",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Action": {
      "main": [
        []
      ]
    }
  },
  "pinData": {},
  "meta": {
    "instanceId": "1795e0dd65e3ea6d4e393ce2383c1be1063c1db87f1a76fa30d08d658771dfd1"
  }
}
